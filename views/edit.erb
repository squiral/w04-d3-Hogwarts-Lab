<form action="/students/<%= @student.id %>" method="post">

<label for="first-name-field">First Name:</label>
<input type="text"
name="first_name"
id="first-name-field"
value="<%= @student.first_name %>"
/>

<label for="last-name-field">Last Name:</label>
<input type="text"
name="last_name"
id="last-name-field"
value="<%= @student.last_name %>"
/>

<label for="age-field">Age:</label>
<input type="number"
 name="age"
 id="age-field"
 value="<%= @student.age %>"
 />


<label for="house_select">Select House:</label>
<select name="house_id" id="house_select" selected="<%= @student.house_id %>">
  <% @houses.each do |house| %>
    <% if house.id == @student.house_id %>
      <option value=<%= house.id %> selected><%= house.name.capitalize %></option>
    <% else %>
      <option value=<%= house.id %>><%= house.name.capitalize %></option>
    <% end %>
  <% end %>


  <!-- <option value="1" <%= "selected" if @student.house.name == "gryffindor" %>
  >Gryffindor</option>
  <option value="2" <%= "selected" if @student.house.name == "hufflepuff" %>
  >Hufflepuff</option>
  <option value="3" <%= "selected" if @student.house.name == "ravenclaw" %>
  >Ravenclaw</option>
  <option value="4" <%= "selected" if @student.house.name == "slytherin" %>
  >Slytherin</option> -->
</select>

<input type="submit" value="Update Student">

</form>
